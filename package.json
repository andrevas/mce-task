{
  "name": "mce-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install && concurrently \"cd backend && npm install && npm run build\" \"cd frontend && npm install && npm run build\" \"cd ..\"",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
